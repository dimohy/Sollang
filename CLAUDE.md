# Sollang Agent entry point

Read and follow `AGENTS.md`, then read `docs/AI_SLG_BEST_PRACTICES.md` before
writing Sollang code or changing the compiler. Follow its references to
`docs/SPEC.md`; update the relevant specification when behavior changes.
The guide is for onboarding and usage, not progress records. Update
`docs/SESSION_HANDOFF.md` only when the user explicitly requests a handoff.
