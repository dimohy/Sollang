# Integer literals inherit fixed-width types from their struct fields.
struct Packet {
    first: UInt8
    second: UInt8
}

main {
    Packet { first: 65, second: 90 } => packet
    "packet=$(packet.first),$(packet.second)" -> println
}
