import sollang.compiler.llvm.text as llvm

main {
    [
        """
        outer context: Int -> Int {
            inner value: Int -> Int {
                context + value
            }
            3 -> inner
        }

        main { }
        """,
        ~
    ] => sources!
    sources! -> llvm.emit
}
