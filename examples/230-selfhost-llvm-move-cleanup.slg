import sollang.compiler.llvm.text as llvm

main {
    [
        """
        consume marker: Int, values: move [Int; ~] -> Int {
            41
        }

        choose flag: Bool -> Int {
            [41, ~] => values
            consume(0, values) => result
            result
        }

        main {
            choose(true) => result
        }
        """,
        ~
    ] => sources!
    sources! -> llvm.emit
}
