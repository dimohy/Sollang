import sollang.compiler.llvm.text as llvm

main {
    [
        """
        select value: Int -> Int {
            value == 0 -> if { 10 } else { 20 } => selected
            selected + 1
        }

        main { }
        """,
        ~
    ] => sources!
    sources! -> llvm.emit
}
