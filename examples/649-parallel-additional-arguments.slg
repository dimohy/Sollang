addOffset value: Int, offset: Int -> Int {
    value + offset
}

main {
    10 => offset
    [1, 2, 3, 4, ~] -> parallel value {
        value -> addOffset(offset)
    } => shifted!

    shifted! -> each value {
        "$value" -> println
    }
}
