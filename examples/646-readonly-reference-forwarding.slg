struct Pair {
    left: Int
    right: Int
}

sum value: ref Pair -> Int {
    value.left + value.right
}

forward value: ref Pair -> Int {
    value -> sum
}

main {
    Pair { left: 20, right: 22 } => pair
    pair -> forward => total
    "$total" -> println
}
