define internal ptr @smalllang_fn_Point_boxed(ptr %stdin, ptr %stdout, ptr %written, ptr %read, ptr %ok_state)
define internal i32 @smalllang_fn_consume(ptr %stdin, ptr %stdout, ptr %written, ptr %read, ptr %ok_state, ptr %it)
call ptr @smalllang_alloc(i64 8)
call void @smalllang_free(ptr
