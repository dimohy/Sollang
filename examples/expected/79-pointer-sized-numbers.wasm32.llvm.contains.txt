%smalllang.struct.1024 = type { i32, i32 }
define internal i32 @smalllang_fn_advance(ptr %stdin, ptr %stdout, ptr %written, ptr %read, ptr %ok_state, i32 %it)
define internal i32 @smalllang_fn_remaining(ptr %stdin, ptr %stdout, ptr %written, ptr %read, ptr %ok_state, i32 %it)
sext i32
zext i32
