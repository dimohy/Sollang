import sollang.compiler.llvm.text as llvm

main {
    [
        """
        main {
            "hello" => message
            message -> println
        }
        """,
        ~
    ] => sources!
    sources! -> llvm.emit
}
