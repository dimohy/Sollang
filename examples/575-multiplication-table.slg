# Verifies nested ranges, the contextual it binding, and expression interpolation.
main {
    2..9 -> each dan {
        "$(dan)단" -> println

        1..9 -> each {
            "$dan × $it = $(dan * it)" -> println
        }

        "" -> println
    }
}
