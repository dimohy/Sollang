struct OwnedKey {
    id: Int
    payload: box Int
}

trait Hash {
    hash: self -> Int
}

trait Eq {
    eq: self -> Int
}

impl Hash for OwnedKey {
    hash: self -> Int => self.id
}

impl Eq for OwnedKey {
    eq: self -> Int => self.id
}

main {
    OwnedKey { id: 1, payload: box 10 } => storedKey!
    {OwnedKey: Int; storedKey!: 100} => values!
    storedKey!.id => invalid
}
