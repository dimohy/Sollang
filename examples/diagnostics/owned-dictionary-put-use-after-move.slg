struct OwnedValues {
    values: [Int; ~]
}

main {
    {Int: OwnedValues; 1: { values: [10, ~] }} => batches!
    OwnedValues { values: [7, 8, 9, ~] } => replacement!
    batches! -> put(1, replacement!)
    replacement!.values -> len => invalid
}
