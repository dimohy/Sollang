formatLine value: Int, storage: mut Arena -> Text {
    "stable $value" -> materialize(storage)
}

main {
    Arena(0) => storage!
    1 -> formatLine(storage!) => text
    storage! -> reset
    text -> println
}
