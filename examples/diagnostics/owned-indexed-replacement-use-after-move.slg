struct OwnedValues {
    values: [Int; ~]
}

main {
    [OwnedValues; { values: [1, ~] }, ~] => batches!
    OwnedValues { values: [7, 8, 9, ~] } => replacement!
    replacement! => batches![0]
    replacement!.values -> len => invalid
}
