struct OwnedSymbol {
    name: Text
    payload: [Int; ~]
}

main {
    {Int: OwnedSymbol;
        1: OwnedSymbol { name: "lexer", payload: [10, ~] }
    } => symbols!
    symbols![1] => escaped!
    escaped!.name -> println
}
