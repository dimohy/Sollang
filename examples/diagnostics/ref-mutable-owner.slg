# A mutable owner cannot be borrowed before the CFG owner lock is available.
struct Pair {
    first: Int
}

first pair: ref Pair -> ref Int {
    pair.first
}

main {
    Pair { first: 41 } => pair!
    pair! -> first => borrowed
}
