# A reference-bearing struct cannot escape a callee-owned local.
struct PairView {
    value: ref Int
}

same value: ref Int -> ref Int {
    value
}

bad: -> PairView {
    10 => local
    PairView { value: local -> same }
}

main { }
