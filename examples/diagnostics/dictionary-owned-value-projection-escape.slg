struct OwnedSymbol {
    name: Text
    payload: [Int; ~]
}

main {
    {Int: OwnedSymbol;
        1: OwnedSymbol { name: "lexer", payload: [10, 20, ~] }
    } => symbols!
    symbols![1].payload => escaped!
    escaped! -> len -> println
}
