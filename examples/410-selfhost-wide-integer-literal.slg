import sollang.compiler.llvm.text as llvm

main {
    [
        """
        wide: -> UInt64 => UInt64(15528987723426532771)

        main {
            wide => value
        }
        """,
        ~
    ] => sources!
    sources! -> llvm.emit
}
