# Cold merge requests one available item from each input in fair product order.
import std.sequence

makeValues values: Range -> Stream<Int> {
    values -> defer
}

main {
    1..3 -> makeValues => first
    10..12 -> makeValues => second
    (first, second) -> merge => merged
    merged -> each value {
        "$value" -> println
    }
}
