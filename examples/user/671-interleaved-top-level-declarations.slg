# Declarations may be interleaved freely after namespace/import declarations.
struct Value {
    number: Int
}

make: -> Value => Value { number: 40 }

enum Mode {
    Add
}

finish value: Value -> Int => value.number + 2

main {
    make() -> finish => result
    "$result" -> println
}
