# Parallel branch overlaps only pure Copy or readonly arms and rejoins by label order.
double value: Int -> Int => value * 2
square value: Int -> Int => value * value

main {
    7 -> parallel branch {
        doubled: ref -> double
        squared: ref -> square
    } => results

    "doubled=$(results.doubled)" -> println
    "squared=$(results.squared)" -> println
}
