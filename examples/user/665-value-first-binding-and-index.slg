# Every definition reads value-first, including indexed mutation.
main {
    [10, 20, 30; ~] => values!
    99 => values![1]
    values![0] + values![1] + values![2] => total
    "$total" -> println
}
