import sollang.compiler.llvm.text as llvm

main {
    [
        """
        show value: Text -> Unit {
            value -> println
        }
        main {
            show("hello")
        }
        """,
        ~
    ] => sources!
    sources! -> llvm.emit
}
