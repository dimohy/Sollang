struct IndexedInner {
    data: [Int; ~]
}
struct IndexedFrame {
    inner: IndexedInner
}
struct IndexedOwner {
    frames: [IndexedFrame; ~]
}
observeIndexedData data: [Int] -> Int => data -> len
main {
    IndexedOwner { frames: [IndexedFrame { inner: IndexedInner { data: [7, 8,; ~] } },; ~] } => owner
    owner.frames[0].inner.data -> observeIndexedData => count
    "$count,$(owner.frames[0].inner.data[0])" -> println
}
