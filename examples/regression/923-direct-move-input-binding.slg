struct Packet {
    bytes: [UInt8; 4]
}

relay packet: move Packet -> Packet {
    packet => owned
    owned
}

main {
    Packet { bytes: [1, 2, 3, 4] } => packet
    packet -> relay => received
    "move-input=$(received.bytes[0]),$(received.bytes[3])" -> println
}
