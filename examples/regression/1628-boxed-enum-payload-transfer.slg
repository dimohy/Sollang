enum Owner {
    Empty
    Value(box Int)
}
consume value: move box Int -> Int { 13 }
finish owner: move Owner -> Int {
    owner -> when {
        Empty { 0 }
        Value(value) { value -> consume }
    }
}
main { Owner.Value(box 13) -> finish -> println }
