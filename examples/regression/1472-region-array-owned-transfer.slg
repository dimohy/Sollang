struct Owner { values: [Int; ~] }
sink values: move [Owner; ~] -> Int { values -> len }
consume value: move Owner -> Int {
    true -> if { [value,; ~] => items
        items -> sink -> return }
    0
}
main { Owner { values: [7,; ~] } -> consume -> println }
