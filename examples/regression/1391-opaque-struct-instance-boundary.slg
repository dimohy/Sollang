import sample.opaque_value as opaque

main {
    UInt64(7) -> opaque.token => token
    "value=$(token -> value)" -> println
    UInt64(8) -> opaque.peerToken => peer
    "peer=$(peer -> value)" -> println
    opaque.Point { x: 9 } => point!
    10 => point!.x
    "public=$(point!.x)" -> println
}
