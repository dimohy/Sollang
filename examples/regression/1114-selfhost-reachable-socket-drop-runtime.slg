import std.net.socket as socket
import sys.io

public struct SocketOwner {
    public socket: socket.UdpSocket
    public port: UInt16
}

impl SocketOwner {
    public localPort: self -> UInt16 => self.port
}

main {
    "socket-drop-runtime" -> println
}
