import sys.process as process

argumentCount: -> UIntSize uses Process {
    process.arguments() -> len
}

main {
    argumentCount() => count
    count > UIntSize(0) -> if {
        "process-arguments-function-ok" -> println
    } else {
        "process-arguments-function-missing" -> println
    }
}
