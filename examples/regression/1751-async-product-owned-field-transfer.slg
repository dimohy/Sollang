enum FailureKind { Capacity }

observe: -> async Unit uses Console {
    [7; ~] => values
    (values, FailureKind.Capacity) => failure
    failure._0 => recovered
    yield
    recovered -> len => count
    "$count" -> println
}

main {
    observe() => observation
    observation -> await
}
