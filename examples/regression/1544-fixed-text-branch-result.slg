# Both branches preserve the fixed Text array representation.
read selected: Bool -> Text {
    selected -> if { ["left"] } else { ["right"] } => values
    values[0]
}
main {
    true -> read -> println
    false -> read -> println
}
