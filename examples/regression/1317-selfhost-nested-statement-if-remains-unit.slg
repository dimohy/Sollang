main {
    UIntSize(0) => first!
    UIntSize(0) => second!
    true -> if {
        true -> if {
            7 => first!
        } else {
            false -> if {
                9 => second!
            }
        }
    }
    "first=$(first!),second=$(second!)" -> println
}
