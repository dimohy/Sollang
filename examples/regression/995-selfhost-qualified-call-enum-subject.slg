import sys.crypto.random

# A qualified call result, not its target-name component, is the enum subject.
main {
    random.bytes(8) -> when {
        Ok(bytes) { "qualified enum subject=$(bytes -> len)" -> println }
        Err(error) { "qualified enum subject error" -> println }
    }
}
