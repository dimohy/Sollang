import sollang.compiler.llvm.text as llvm

# A call argument is converted to the declared parameter width before the call.
main {
    [
        """
        platformOffset value: UIntSize -> UIntSize => value

        main {
            7 => offset!
            offset! -> platformOffset -> println
        }
        """; ~
    ] => sources
    sources -> llvm.emit
}
