make: -> [UInt8; 1] { [13] }
copy values: move [UInt8; 1] -> [UInt8; 1] { values -> return }
first values: [UInt8] -> UInt8 { values[0] }
main {
    make() => source
    source -> copy => named
    named -> first -> println
}
