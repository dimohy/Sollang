enum Failure { Bad }

wrap<T> value: T -> Result<T, (T, Failure)> {
    Result<T, (T, Failure)>.Ok(value)
}

main {
    7 -> wrap<Int> -> when {
        Ok(value) { value -> println }
        Err(failure) { failure._0 -> println }
    }
}
