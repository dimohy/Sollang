# A borrowed call releases its temporary owning record after the last use.
struct Owner { values: [Int; ~] }
create: -> Owner { Owner { values: [13; ~] } }
impl Owner {
    first: self -> Int { self.values[0] }
}
main {
    create() -> first -> println
}
