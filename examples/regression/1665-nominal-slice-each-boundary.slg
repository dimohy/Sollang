struct Item { value: Int }
show items: [Item] -> Unit uses Console {
    items -> each item { item.value -> println }
    (items -> len) -> println
}
main {
    [] -> show
    [Item { value: 42 }] -> show
}
