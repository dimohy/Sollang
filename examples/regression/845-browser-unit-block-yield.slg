# A Unit-returning block function passes only its declared value to the callback.
notify value: Int -> Unit block item: Int {
    value -> yield
}

main {
    42 -> notify item {
        "notify invoked its user block" -> println
    }
}
