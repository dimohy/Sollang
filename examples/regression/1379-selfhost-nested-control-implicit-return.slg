choose value: Int -> Int {
    value >= 0 -> if {
        value == 0 -> if { 10 } else { 20 }
    } else {
        30
    }
}

main {
    "nested-control-zero=$(choose(0))" -> println
    "nested-control-positive=$(choose(1))" -> println
    "nested-control-negative=$(choose(-1))" -> println
}
