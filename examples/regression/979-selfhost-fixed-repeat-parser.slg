import sollang.compiler.parser as parser

# The generated parser must accept a fixed-repeat array independently of source
# size and surrounding function structure.
main {
    "main { [UInt64(0); 64] => words! }" -> parser.parseEvents => events
    events[(events -> len) - 1] => outcome
    "fixed-repeat parse = $(outcome.value)" -> println
}
