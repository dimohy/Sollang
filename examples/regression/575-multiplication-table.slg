# Verifies nested ranges, contextual it, interpolation, and println() blank lines.
main {
    2..9 -> each dan {
        "$(dan)단" -> println

        1..9 -> each {
            "$dan × $it = $(dan * it)" -> println
        }

        println()
    }
}
