struct Algorithms { }
impl Algorithms {
    identity<T>: self, value: T -> T => value
}
main {
    Algorithms { } -> identity(43) => inferred
    Algorithms { } -> identity<Long>(44) => wide
    Algorithms { } -> identity<Text>("text") => text
    "$inferred,$wide,$text" -> println
}
