make: -> [UInt8; 1] { [13] }

read selected: Bool -> Int {
    selected -> if {
        make() => bytes
        bytes
    } else {
        [UInt8(17)]
    } => bytes
    Int(bytes[0])
}

main {
    true -> read -> println
    false -> read -> println
}
