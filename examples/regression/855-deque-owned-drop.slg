main {
    Deque<box Int>(2) => values!
    values! -> pushBack(box 1)
    values! -> pushBack(box 2)
    values! -> pushFront(box 3)
    values! -> len => length
    "owned len=$length" -> println
}
