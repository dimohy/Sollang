struct Bucket {
    values: [Int; ~]
}

append values: mut [Int; ~], value: Int -> Unit {
    values -> push(value)
}

main {
    Bucket { values: [Int; ~] } => bucket!
    append(bucket!.values, 7)
    bucket!.values -> take(0) => value
    "value=$value" -> println
}
