primary value: Int -> Int {
    true -> if { [value,; ~] => items
        items[0] -> return }
    0
}
additional flag: Bool, value: Int -> Int {
    flag -> if { [value,; ~] => items
        items[0] -> return }
    0
}
main { 7 -> primary -> println
    true -> additional(9) -> println }
