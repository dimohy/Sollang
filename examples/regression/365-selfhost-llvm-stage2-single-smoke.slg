import sollang.compiler.llvm.text as llvm

main {
    [
        """
        # The source root itself owns an implicit executable entry.
        "stage2-single-ok" -> println
        """; ~
    ] => sources!
    sources! -> llvm.emit
}
