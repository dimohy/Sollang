# A repeated fixed-array field keeps its declared shape in the program entry body.
struct Packet {
    bytes: [UInt8; 4]
}

main {
    Packet { bytes: [200; 4] } => packet
    "edge=$(Int(packet.bytes[3]))" -> println
}
