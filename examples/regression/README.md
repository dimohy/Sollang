# Regression examples

This directory is compiler verification material, not the learning path.

- numbered `.slg` files are executable positive cases;
- `expected/` stores stdout, stdin, source lists, and target metadata;
- `diagnostics/` stores negative cases and required diagnostics;
- the remaining directories contain multi-file, browser, package, and project
  fixtures.

Use `../user/` for the compact language tour.
