struct Inner {
    value: Int
}

struct Outer {
    inner: Inner
}

impl Inner {
    public read: self -> Int => self.value
}

# A projected nominal field remains the receiver of its following instance call.
main {
    Outer { inner: Inner { value: 42 } } => outer
    "$(outer.inner -> read)" -> println
}
