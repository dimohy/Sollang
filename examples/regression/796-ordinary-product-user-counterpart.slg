# Ordinary products join several values into one explicit structural input.
sum values: (Int, Int, Int) -> Int => values._0 + values._1 + values._2

main {
    (10, 20, 30) -> sum => total
    "$total" -> println
}
