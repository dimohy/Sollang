# `->` carries values through flow; `=>` defines bodies, arms, and bindings.
square value: Int -> Int => value * value

main {
    5 -> square => result
    result -> when {
        == 25 => "flow=25" -> println
        else => "unexpected" -> println
    }
}
