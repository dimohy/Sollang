import std.uuid

# N001 measures the source spelling, not a semantically expanded qualified
# target path, so this readable short condition stays inline without a note.
main {
    uuid.codec() -> nil -> isNil -> if {
        "short=true" -> println
    }
}
