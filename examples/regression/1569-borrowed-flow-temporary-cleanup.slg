# A function-produced intermediate flow owner is released after borrowing.
struct Owner { values: [Int; ~] }
create value: Int -> Owner { Owner { values: [value, value; ~] } }
impl Owner {
    first: self -> Int { self.values[0] }
}
main {
    13 -> create -> first -> println
}
