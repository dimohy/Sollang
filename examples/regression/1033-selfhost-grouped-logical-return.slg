isMarker byte: UInt8 -> Bool {
    ((byte >= 65 and byte <= 90)
        or byte == 45
        or byte == 46
        or byte == 95
        or byte == 126)
}

main {
    UInt8(126) -> isMarker -> if { "logical-return=ok" -> println }
}
