import sys.process as process

main {
    process.arguments() => args
    (args -> len) -> println
    args -> each argument {
        argument == args[0] -> if {
            (argument -> len) > 0 -> println
        } else { argument -> println }
    }
    (process.arguments() -> len) -> println
}
