main {
    0 => total!
    1..2 -> each value {
        total! + value => total!
    }
    total! == 3
        -> if { "trailing value if preserved" } else { "trailing value if corrupted" }
        -> println
}
