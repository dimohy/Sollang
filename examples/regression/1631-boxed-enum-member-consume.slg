struct Point { x: Int }
enum Owner {
    Empty
    Value(box Point)
}
consume owner: move Owner -> Int { owner -> when { Empty { 0 } Value(point) { point.x } } }
main { Owner.Value(box Point { x: 13 }) -> consume -> println }
