struct Buffer {
    values: [Int; ~]
}

impl Buffer {
    append: mut self, value: Int -> Unit {
        self.values -> push(value)
    }

    takeFirst: mut self -> Int => self.values -> take(0)
}

main {
    0 -> println
}
