choose flag: Bool -> UInt64 => flag -> if {
    true -> if { 9 } else { 8 }
} else {
    7
}

bytes: -> [UInt8; 4] => [0; 4]

main {
    bytes() => values!
    5 => values![0]
    UInt64(7) => counter!
    0 => counter!
    "$(values![0]),$(counter!),$(choose(true))" -> println
}
