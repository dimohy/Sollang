make: -> [UInt8; 1] { [13] }
first values: [UInt8] -> UInt8 { values[0] }
additional marker: Int, values: [UInt8] -> UInt8 { values[0] }
main {
    make() -> first -> println
    first(make()) -> println
    0 -> additional(make()) -> println
}
