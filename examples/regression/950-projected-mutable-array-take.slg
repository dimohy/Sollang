struct Bucket {
    values: [Int; ~]
    marker: Int
}

main {
    Bucket { values: [10, 20; ~], marker: 7 } => bucket!
    bucket!.values -> take(0) => first
    "$first,$(bucket!.values -> len),$(bucket!.marker)" -> println
}
