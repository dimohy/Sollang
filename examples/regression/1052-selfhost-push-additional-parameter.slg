appendWhen output: mut [UInt8; ~], enabled: Bool, value: UInt8 -> Unit {
    enabled -> if { output -> push(value) }
}

main {
    [UInt8; ~] => output!
    appendWhen(output!, true, 65)
    Int(output![0]) -> println
}
