# Moving an owned value into a product transfers it once and drops the product field once.
readTag pair: move ([Int; ~], Int) -> Int => pair._1

main {
    [10, 20; ~] => values!
    (values!, 7) => pair!
    pair! -> readTag => tag
    "$tag" -> println
}
