# In-place mutation followed by borrowing retains the named array owner.
first values: [Int] -> Int { values[0] }
main {
    [13; ~] => values!
    values! -> reserve(1)
    values! -> first -> println
    values! -> first -> println
}
