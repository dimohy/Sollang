struct Pair {
    left: [Int; ~]
    right: [Int; ~]
}

main {
    Pair { left: [11; ~], right: [22; ~] } => original
    Pair { left: original.left, right: original.right } => transferred
    "left=$(transferred.left[0])" -> println
    "right=$(transferred.right[0])" -> println
}
