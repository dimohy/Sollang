# Signed fixed-array indexing sign-extends before an Int formatter consumes it.
struct SignedPacket {
    bytes: [Int8; 4]
}

main {
    Int8(-56) => negative
    SignedPacket { bytes: [negative; 4] } => packet
    "signed=$(Int(packet.bytes[2]))" -> println
}
