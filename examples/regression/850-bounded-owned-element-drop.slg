struct Owned {
    value: box Int
}

makeOwned: -> [Owned; <=4] {
    [Owned { value: box 7 }, Owned { value: box 9 }; <=4] => values!
    values!
}

main {
    makeOwned() => values!
    values! -> len => count
    "$count" -> println
}
