main {
    0 => pass!
    UInt64(0) => total!
    pass! < 2 -> while {
        [UInt64(7); 64] => words
        words[63] + total! => total!
        pass! + 1 => pass!
    }
    "fixed repeat inside while = $(total!)" -> println
}
