# Concat drains every stream in product order without an intermediate buffer.
import std.sequence

makeValues values: Range -> Stream<Int> {
    values -> defer
}

main {
    1..2 -> makeValues => first
    4..5 -> makeValues => second
    (first, second) -> concat => joined
    joined -> each value {
        "$value" -> println
    }
}
