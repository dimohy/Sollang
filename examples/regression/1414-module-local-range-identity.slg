namespace regression.local_range

struct Range {
    count: Int
}

inspect value: Range -> Int => value.count

public probe: -> Int {
    Range { count: 7 } -> inspect
}

main {
    regression.local_range.probe() -> println
}
