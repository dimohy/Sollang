import sys.process as process

identity arguments: Arguments -> Arguments => arguments

main {
    process.arguments() => arguments
    0 => iteration!
    iteration! < 2 and (process.arguments() -> identity -> len) == (arguments -> identity -> len)
        -> while {
            iteration! -> println
            iteration! + 1 => iteration!
        }
}