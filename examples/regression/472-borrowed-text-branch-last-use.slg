import sys.file as file

prefix source: file.SourceText -> Text {
    source -> slice(UIntSize(0), UIntSize(3))
}

consume source: move file.SourceText -> Unit {
}

main {
    "lexer" -> file.borrowText => source!
    source! -> prefix => view
    true -> if {
        view -> println
        source! -> consume
    } else {
        "branch skipped" -> println
        source! -> consume
    }
    "moved after the branch-local last use" -> println
}
