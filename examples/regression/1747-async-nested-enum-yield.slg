produce: -> Result<Int, Text> { Result<Int, Text>.Ok(7) }

work: -> async Int {
    produce() -> when {
        Ok(value) {
            yield
            value
        }
        Err(error) { 0 }
    }
}

main {
    work() => task
    task -> await -> println
}
