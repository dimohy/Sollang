identity<T> value: T -> T => value

struct Algorithms { }
impl Algorithms {
    echo<T>: self, value: T -> T {
        value -> identity
    }
}

main {
    Algorithms { } -> echo(300) -> println
    Algorithms { } -> echo("nested") -> println
}
