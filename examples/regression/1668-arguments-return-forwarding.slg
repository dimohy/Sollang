import sys.process as process

identity arguments: Arguments -> Arguments => arguments

forward arguments: Arguments -> Arguments {
    arguments -> identity
}

current: -> Arguments uses Process {
    process.arguments() -> forward
}

main {
    current() => arguments
    (arguments -> len) -> println
    arguments[1] -> println
    arguments[2] -> println
    (arguments -> identity -> len) -> println
}
