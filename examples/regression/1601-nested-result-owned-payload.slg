# Intrinsic generic enums retain nested owning payloads.
struct Payload { values: [Int; ~] }
main {
    Result<Payload, Int>.Ok(Payload { values: [13; ~] }) => result
    result -> when {
        Ok(payload) { payload.values[0] -> println }
        Err(code) { code -> println }
    }
}
