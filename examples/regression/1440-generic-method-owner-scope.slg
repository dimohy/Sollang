struct First { }
struct Second { }
impl First { identity<T>: self, value: T -> T => value }
impl Second { identity<T>: self, value: T -> T => value }
main {
    First { } -> identity(1) => first
    Second { } -> identity("second") => second
    "$first,$second" -> println
}
