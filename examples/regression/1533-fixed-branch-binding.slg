make: -> [UInt8; 1] { [13] }
read selected: Bool -> Int {
    0 => result!
    selected -> if {
        make() => bytes
        Int(bytes[0]) => result!
    }
    result!
}
main {
    true -> read -> println
    false -> read -> println
}
