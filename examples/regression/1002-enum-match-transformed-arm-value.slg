convert result: Result<Int32, Int32> -> Int32 {
    result -> when {
        Ok(value) => value
        Err(error) => -error
    }
}

main {
    Result<Int32, Int32>.Err(1) -> convert => value
    "transformed arm = $value" -> println
}
