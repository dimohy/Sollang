import std.sequence
main {
    1..2 -> map index { index + 10 }
        -> flatMap(1..2) outer, inner { "$outer:$inner:$(outer + inner)" }
        -> take(2)
        -> each value { value -> println }
}
