enum Frame {
    Data(Int)
    Padding
}

main {
    [Frame.Data(7),; ~] => frames!
    frames! -> take(0) -> when {
        Data(value) { value }
        Padding { 0 }
    } => selected
    selected -> println
}
