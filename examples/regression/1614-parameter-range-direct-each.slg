render start: Int, endInclusive: Int -> Unit uses Console {
    start..endInclusive -> each value {
        value -> println
    }
}

main {
    1 -> render(2)
    3 -> render(2)
    2 -> render(3)
}
