# Both branches preserve the fixed Int array representation.
read selected: Bool -> Int {
    selected -> if { [13, 14] } else { [17, 18] } => values
    values[0] + values[1]
}
main {
    true -> read -> println
    false -> read -> println
}
