# A consuming call takes responsibility for its temporary receiver exactly once.
struct Owner { values: [Int; ~] }
create: -> Owner { Owner { values: [13; ~] } }
impl Owner {
    first: move self -> Int { self.values[0] }
}
main {
    create() -> first -> println
}
