namespace audit.leaf

public struct Hasher { value: Int }

public create value: Int -> Hasher { Hasher { value: value } }

impl Hasher {
    public finish: move self -> Int { self.value }
    public read: self -> Int { self.value }
    public run: self -> Int { self -> read }
}
