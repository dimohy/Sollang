namespace examples.projected_binding_runtime

import examples.projected_binding_type as model

public closeSession session: move model.Session -> UInt32 {
    session.connection => connection
    connection -> close(7)
}
