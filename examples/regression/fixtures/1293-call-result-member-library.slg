namespace sample.call_result_member

import sample.call_result_member_model as model

public struct Identity {
    public direction: model.Direction
}

public identity id: UInt64 -> Identity {
    Identity { direction: model.Direction.Unidirectional }
}
