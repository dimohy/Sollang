namespace sample.returned_each

public struct Item {
    public value: Int
}

public make count: Int -> [Item; ~] {
    [Item; ~] => items!
    0 => index!
    index! < count -> while {
        items! -> push(Item { value: index! + 1 })
        index! + 1 => index!
    }
    items!
}
