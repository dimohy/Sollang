namespace examples.qualified_impl_runtime

import examples.qualified_impl_type as net

public struct Stream {
    public token: UInt64
}

impl net.Endpoint {
    public connect: self -> Stream => Stream { token: self.token }
}
