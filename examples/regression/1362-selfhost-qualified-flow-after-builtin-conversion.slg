namespace examples.regression.selfhost_qualified_flow_after_builtin_conversion

import std.crypto.bits

main {
    UInt32(0) => shift
    8 / (UInt32(shift) -> bits.powerOfTwo) => value
    "$value" -> println
}
