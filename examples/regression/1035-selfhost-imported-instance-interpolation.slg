import fixture.result_instance as counters

main {
    counters.create(41) -> when {
        Ok(counter) { "value=$(counter -> read)" -> println }
        Err(error) { "error=$error" -> println }
    }
}
