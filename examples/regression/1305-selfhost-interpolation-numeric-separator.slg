# LLVM integer constants never retain Sollang's visual '_' separators.
identity value: Int -> Int => value

main {
    "literal-call=$(identity(4_096))" -> println
    "literal-binary=$(4_000 + 96)" -> println
    "literal-unary=$(-1_024)" -> println
}
