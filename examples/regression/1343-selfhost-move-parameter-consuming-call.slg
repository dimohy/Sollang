struct Owner {
    bytes: [UInt8; ~]
}

consume owner: move Owner -> Int {
    owner.bytes -> len
}

forward owner: move Owner -> Int {
    owner -> consume
}

main {
    Owner { bytes: [1, 2, 3; ~] } -> forward => count
    "move-parameter-consuming-call=$(count == 3)" -> println
}
