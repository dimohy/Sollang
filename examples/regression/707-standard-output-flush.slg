import sys.runtime as runtime

# Explicit flush is a real native I/O barrier used before the next blocking
# protocol read; it does not add bytes or change their order.
main {
    "ready:" -> print
    runtime.flushStandardOutput()
    "done" -> println
}
