# An empty recursive heap owner has finite inline storage.
enum Tree {
    Leaf
    Children([Tree; ~])
}
main {
    Tree.Children([Tree; ~]) => tree
    tree -> when {
        Leaf { "leaf" -> println }
        Children(children) { children -> len -> println }
    }
}
