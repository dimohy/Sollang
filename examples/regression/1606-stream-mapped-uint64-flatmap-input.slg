import std.sequence
wide: -> UInt64 { 4294967296 }
main {
    1..2 -> map index { wide() }
        -> flatMap(1..2) outer, inner { "$(outer):$(inner)" }
        -> take(2)
        -> each value { value -> println }
}
