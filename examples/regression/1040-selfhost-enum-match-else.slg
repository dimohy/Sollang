enum Choice {
    Known
    Other
}

label choice: Choice -> Int {
    choice -> when {
        Known { 1 }
        else { 2 }
    }
}

main {
    label(Choice.Known) => known
    label(Choice.Other) => other
    "$known,$other" -> println
}
