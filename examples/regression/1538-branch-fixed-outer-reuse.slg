make: -> [UInt8; 1] { [13] }

read selected: Bool -> Int {
    make() => original
    selected -> if {
        original
    } else {
        [UInt8(17)]
    } => selectedBytes
    Int(selectedBytes[0]) + Int(original[0])
}

main {
    true -> read -> println
    false -> read -> println
}
