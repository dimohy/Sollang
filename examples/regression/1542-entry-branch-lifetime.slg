make: -> [UInt8; 1] { [13] }

main {
    true -> if {
        make() => bytes
        bytes[0] -> println
    }
    false -> if {
        make() => bytes
        bytes[0] -> println
    } else {
        make() => bytes
        bytes[0] -> println
    }
}
