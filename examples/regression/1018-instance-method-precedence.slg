struct Counter {
    value: Int
}

impl Counter {
    add: self, amount: Int -> Int => self.value + amount
}

add counter: Counter, amount: Int -> Int => 900 + amount

main {
    Counter { value: 40 } -> add(2) => result
    "method=$result" -> println
}
