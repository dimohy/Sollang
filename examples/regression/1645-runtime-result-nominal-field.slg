struct Holder {
    value: sys.directory.RawResult
}
main {
    Holder { value: sys.directory.RawResult.Err("missing") } => holder
    holder.value -> when {
        Ok(raw) { "ok" -> println }
        Err(message) { message -> println }
    }
}
