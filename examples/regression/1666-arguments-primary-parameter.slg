import sys.process as process

show arguments: Arguments -> Unit uses Console {
    arguments -> each argument {
        argument == arguments[0] -> if {
            (argument -> len) > 0 -> println
        } else { argument -> println }
    }
}

main {
    process.arguments() => arguments
    arguments -> show
    (arguments -> len) -> println
}
