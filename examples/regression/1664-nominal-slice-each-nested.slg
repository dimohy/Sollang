struct Item { value: Int }
show items: [Item] -> Unit uses Console {
    1..2 -> each round {
        items -> each item { (item.value + round) -> println }
    }
}
main { [Item { value: 13 }, Item { value: 27 }] -> show }
