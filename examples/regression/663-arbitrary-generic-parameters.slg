# Arbitrary generic parameters are inferred from every ordinary argument.
chooseFourth<A, B, C, D> first: A, second: B, third: C, fourth: D -> D {
    fourth
}

main {
    chooseFourth(1, true, 3, Int64(44)) => selected
    "$selected" -> println
}
