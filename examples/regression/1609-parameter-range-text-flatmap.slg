import std.sequence
render start: Int -> Unit uses Console {
    start..2 -> map index { "outer" }
        -> flatMap(1..2) outer, inner { "$(outer):$(inner)" }
        -> take(2)
        -> each value { value -> println }
}
main { 1 -> render }
