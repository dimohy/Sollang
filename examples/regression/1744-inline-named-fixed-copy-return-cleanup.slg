struct FixedCopyHolder {
    bytes: [UInt8; 16]
}
makeFixedBytes: -> [UInt8; 16] => [9; 16]
makeFixedCopyHolder: -> FixedCopyHolder {
    makeFixedBytes() => bytes
    FixedCopyHolder { bytes: bytes }
}
main {
    makeFixedCopyHolder() => holder
    holder.bytes[0] -> println
}
