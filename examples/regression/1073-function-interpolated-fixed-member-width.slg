# Function interpolation consumes the converted index result exactly once.
struct Packet {
    bytes: [UInt8; 4]
}

writeHalf packet: Packet -> Unit uses Console {
    "half=$(Int(packet.bytes[2]) / 2)" -> println
}

main {
    Packet { bytes: [200, 200, 200, 200] } -> writeHalf
}
