import sys.process as process

struct Invocation { arguments: Arguments }

main {
    Invocation { arguments: process.arguments() } => invocation
    (arguments: invocation.arguments, valid: true) => observed
    (observed.arguments -> len) -> println
    observed.arguments[1] -> println
    observed.arguments[2] -> println
    observed.valid -> println
}
