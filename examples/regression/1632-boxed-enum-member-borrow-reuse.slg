struct Point { x: Int }
enum Owner {
    Empty
    Value(box Point)
}
read owner: Owner -> Int { owner -> when { Empty { 0 } Value(point) { point.x } } }
main { Owner.Value(box Point { x: 13 }) => owner
owner -> read -> println
owner -> read -> println }
