# Tap runs its side flow and preserves the original value for the following stage.
audit value: Int -> Unit uses Console {
    "audit=$value" -> println
}

double value: Int -> Int => value * 2

main {
    7 -> tap audit -> double => result
    "result=$result" -> println
}
