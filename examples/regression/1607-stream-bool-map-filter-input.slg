import std.sequence
main {
    1..3 -> map index { index > 1 }
        -> filter value { not value }
        -> flatMap(1..2) outer, inner { "$(outer):$(inner)" }
        -> take(2)
        -> each value { value -> println }
}
