import regression.constant_records as records

main {
    records.make() -> when {
        Ok(expansion) {
            expansion.constants -> each value {
                "$(value.value):$(value.sourceNode)" -> println
            }
        }
        Err(_) { "unexpected-error" -> println }
    }
}
