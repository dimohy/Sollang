import sys.file as file

capturedPrefix source: file.SourceText -> Text {
    read: -> Text {
        source -> slice(UIntSize(0), UIntSize(3))
    }

    read()
}

main {
    "lexer" -> file.borrowText => source
    source -> capturedPrefix -> println
}
