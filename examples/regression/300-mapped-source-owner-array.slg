main {
    [MappedBytes; ~] => sources!
    map read "examples/regression/01-function-basic-hello.slg" => source!
    sources! -> push(source!)
    sources! -> len => count
    "mapped sources = $count" -> println
}
