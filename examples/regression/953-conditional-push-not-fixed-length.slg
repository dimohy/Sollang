main {
    [Int; ~] => values!
    true -> if {
        values! -> push(7)
    }
    (values! -> len) == 1 -> if {
        "conditional push remains growable" -> println
    } else {
        "conditional push failed" -> println
    }
}
