identity value: UInt64 -> UInt64 => value
add base: UInt64, offset: UInt64 -> UInt64 => base + offset

main {
    identity(true -> if { 18 } else { 19 }) => first
    add(first, false -> if { 18 } else { 19 }) -> println
}
