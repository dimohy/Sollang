make: -> [UInt8; 1] { [13] }
first values: [UInt8] -> UInt8 { values[0] }
main {
    0 => index!
    index! < 2 -> while {
        make() => named
        named -> first -> println
        index! + 1 => index!
    }
}
