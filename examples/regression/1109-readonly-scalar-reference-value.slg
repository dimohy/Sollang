# A readonly scalar reference participates in value expressions through the
# referenced scalar, never through its storage address.
less current: ref Int, previous: ref Int -> Bool => current < previous

main {
    2 => current
    4 => previous
    current -> less(previous) => descending
    "descending=$descending" -> println
}
