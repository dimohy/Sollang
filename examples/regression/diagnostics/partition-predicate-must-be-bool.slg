# Every non-else partition route requires a Bool predicate.
import std.sequence

makeValues values: Range -> Stream<Int> => values -> defer

main {
    1..4 -> makeValues => values!
    values! -> partition value {
        invalid: when value
        other: else
    } => routed!
}
