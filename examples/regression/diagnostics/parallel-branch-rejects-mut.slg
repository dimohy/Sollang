# A mutable borrow cannot overlap another parallel arm.
struct Item {
    value: Int
}

change item: mut Item -> Int => item.value
inspect item: ref Item -> Int => item.value

main {
    Item { value: 4 } => item!
    item! -> parallel branch {
        changed: mut -> change
        inspected: ref -> inspect
    } => results
}
