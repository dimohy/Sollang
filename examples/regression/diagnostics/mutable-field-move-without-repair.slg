struct Pair {
    left: [Int; ~]
    right: [Int; ~]
}

impl Pair {
    invalid: mut self -> Int {
        self.left => left
        left -> len
    }
}

main {
    Pair { left: [1; ~], right: [2; ~] } => pair!
    pair! -> invalid => result
}
