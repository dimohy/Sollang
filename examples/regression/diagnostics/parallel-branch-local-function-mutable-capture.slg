# A local target cannot hide a mutable capture inside a parallel branch worker.
run value: Int -> (left: Int, right: Int) {
    addOffset input: Int -> Int => input + offset!

    10 => offset!
    value -> parallel branch {
        left: -> addOffset
        right: -> addOffset
    }
}

main {
    1 -> run => result
}
