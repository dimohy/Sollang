import std.net.socket as socket

closeThenReuse connection: move socket.TcpStream -> Result<Unit, socket.SocketError> uses Network {
    connection -> close
    connection -> shutdown(socket.ShutdownDirection.Both)
}

main {
}
