struct Owner { values: [Int; ~] }
read flag: Bool, value: ref Owner -> Int {
    true -> if { [value,; ~] => items
        items -> len -> return }
    0
}
main { Owner { values: [7,; ~] } => value
    true -> read(value) -> println }
