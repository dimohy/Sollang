# Partition route labels are unique result fields.
import std.sequence

makeValues values: Range -> Stream<Int> => values -> defer

main {
    1..4 -> makeValues => values!
    values! -> partition value {
        same: when value % 2 == 0
        same: else
    } => routed!
}
