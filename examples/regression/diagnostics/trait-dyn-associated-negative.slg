trait Measure {
    type Item
    measure: self -> Int
}
struct Point { }
impl Measure for Point {
    type Item = Int
    measure: self -> Int { 1 }
}
main {
    Point { } -> dyn<Measure> => value
}
