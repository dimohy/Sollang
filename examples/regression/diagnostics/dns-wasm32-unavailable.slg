import std.net.dns as dns

main {
    dns.resolver(dns.AddressFamily.Any, 16, 253)
        -> lookup("localhost", 443)
        -> when {
            Ok(endpoints) { }
            Err(error) { }
        }
}
