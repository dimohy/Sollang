# Partition routing is exhaustive through one final else route.
import std.sequence

makeValues values: Range -> Stream<Int> => values -> defer

main {
    1..4 -> makeValues => values!
    values! -> partition value {
        even: when value % 2 == 0
        positive: when value > 0
    } => routed!
}
