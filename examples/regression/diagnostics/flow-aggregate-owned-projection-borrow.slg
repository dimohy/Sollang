struct Owner {
    values: [Int; ~]
    marker: Int
}

struct Request {
    values: [Int; ~]
}

consume request: move Request -> Unit { }

inspect owner: Owner -> Unit {
    Request { values: owner.values } -> consume
    owner.marker -> println
}

main { }
