# Parallel branch rejects observable effects whose ordering would be hidden.
observe value: Int -> Int uses Console {
    "$value" -> println
    value
}

double value: Int -> Int => value * 2

main {
    4 -> parallel branch {
        observed: ref -> observe
        doubled: ref -> double
    } => results
}
