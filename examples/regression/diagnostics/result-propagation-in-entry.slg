struct Value {
    number: Int
}

struct Holder {
    value: Value
}

makeHolder: -> Result<Holder, Int> {
    Result<Holder, Int>.Ok(Holder { value: Value { number: 42 } })
}

main {
    makeHolder()? => holder
    "value=$(holder.value.number)" -> println
}
