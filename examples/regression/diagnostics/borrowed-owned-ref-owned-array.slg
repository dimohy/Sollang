struct Owner { values: [Int; ~] }
read value: ref Owner -> Int {
    true -> if { [value,; ~] => items
        items -> len -> return }
    0
}
main { Owner { values: [7,; ~] } => value
    value -> read -> println }
