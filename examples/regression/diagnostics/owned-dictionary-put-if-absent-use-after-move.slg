struct OwnedValues {
    values: [Int; ~]
}

main {
    {Int: OwnedValues; 1~} => batches!
    OwnedValues { values: [7, 8, 9; ~] } => candidate!
    batches! -> putIfAbsent(1, candidate!) => inserted
    candidate!.values -> len => invalid
}
