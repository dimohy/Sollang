# An affine branch input can be consumed by at most one arm.
consume values: move [Int; ~] -> Int => values -> len

main {
    [1, 2; ~] => values!
    values! -> branch {
        first: move -> consume
        second: move -> consume
    } => result
}
