struct Payload { values: [Int; ~] }
enum Owner {
    Empty
    Value(Result<Payload, Text>)
}
copy owner: Owner -> Owner {
    owner -> when {
        Empty { Owner.Empty }
        Value(result) { Owner.Value(result) }
    }
}
main { }
