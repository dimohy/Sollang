struct Owner { bytes: [UInt8; ~] }
struct Pair { first: Owner, second: Owner }

main {
    Pair { first: Owner { bytes: [1; ~] }, second: Owner { bytes: [2; ~] } } => pair
    pair.first => first
    pair.first => duplicate
    first.bytes -> len -> println
    duplicate.bytes -> len -> println
}
