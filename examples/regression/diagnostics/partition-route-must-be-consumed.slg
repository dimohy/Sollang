# Every partition route is affine and must have one downstream consumer.
import std.sequence

makeValues values: Range -> Stream<Int> => values -> defer

main {
    1..3 -> makeValues => values
    values -> partition value {
        even: when value % 2 == 0
        other: else
    } => routed
    routed.even -> each value {
        "$value" -> println
    }
}
