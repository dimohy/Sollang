main {
    [11, 22; ~] -> Result<[Int; ~], Int>.Ok => outcome
    outcome -> when {
        Ok(values) { values }
        Err(_) { [Int; ~] }
    } => selected
    outcome -> when {
        Ok(values) { "$(values -> len)" -> println }
        Err(error) { "$error" -> println }
    }
}
