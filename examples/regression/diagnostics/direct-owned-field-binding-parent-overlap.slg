struct Owner { bytes: [UInt8; ~] }
struct Branch { first: Owner, second: Owner }

main {
    Branch { first: Owner { bytes: [1; ~] }, second: Owner { bytes: [2; ~] } } => branch
    branch.first => first
    branch => whole
    first.bytes -> len -> println
    whole.second.bytes -> len -> println
}
