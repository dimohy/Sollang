struct Payload { values: [Int; ~] }
enum Owner {
    Empty
    Value(Option<Payload>)
}
copy owner: Owner -> Owner {
    owner -> when {
        Empty { Owner.Empty }
        Value(option) { Owner.Value(option) }
    }
}
main { }
