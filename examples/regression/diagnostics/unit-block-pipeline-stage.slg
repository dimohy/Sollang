# A Unit block cannot feed a later block pipeline stage.
observe value: Int -> Unit block item: Int {
    value -> yield
}

tap value: Int -> Int block item: Int {
    value -> yield
    value
}

main {
    1
        -> observe { "observed=$it" -> println }
        -> tap { "tap=$it" -> println }
        => result
}
