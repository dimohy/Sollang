# Every branch arm must contribute one field to the resulting product.
discard value: Int -> Unit uses Console => "$value" -> println
identity value: Int -> Int => value

main {
    1 -> branch {
        discarded: -> discard
        kept: -> identity
    } => result
}
