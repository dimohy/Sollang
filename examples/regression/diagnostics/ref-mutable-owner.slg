# A mutable owner cannot change while a readonly reference remains live.
struct Pair {
    first: Int
}

first pair: ref Pair -> ref Int {
    pair.first
}

main {
    Pair { first: 41 } => pair!
    pair! -> first => borrowed
    42 => pair!.first
    "$(borrowed)" -> println
}
