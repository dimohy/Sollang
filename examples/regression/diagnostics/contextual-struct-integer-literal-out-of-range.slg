# A contextual UInt8 field still rejects a literal outside 0..255.
struct Packet {
    first: UInt8
    second: UInt8
}

main {
    Packet { first: 256, second: 90 } => packet
    packet.first -> println
}
