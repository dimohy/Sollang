struct Owned {
    value: box Int
}

main {
    # Explicit form: [Owned { value: box "wrong" }, Owned { value: box 20 }]
    [Owned; { value: box "wrong" }, { value: box 20 }] => values
    values -> len => count
    "owned count = $count" -> println
}
