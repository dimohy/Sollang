import sys.input.mouse

main {
    mouse.Source {
        capacity: 8
        overflow: mouse.OverflowPolicy.DropOldest
    } -> events => events
    events -> each event {
        stop
    }
    events -> each event {
        stop
    }
}
