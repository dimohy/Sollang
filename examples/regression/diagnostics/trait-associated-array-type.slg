# Matching storage does not permit a different associated element type.
trait Counter {
    type Item
    count: self, values: [Item; ~] -> Int
}
struct IntCounter { }
impl Counter for IntCounter {
    type Item = Int
    count: self, values: [Text; ~] -> Int { values -> len }
}
main { }
