struct Point {
    x: Int
}

trait Measure {
    measure: self -> Int
}

measureOf<T> value: T -> Int where T: Measure {
    value -> Measure.measure
}

main {
    Point { x: 1 } -> measureOf => result
}
