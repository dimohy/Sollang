# Branch labels are unique fields in the resulting labeled product.
identity value: Int -> Int => value

main {
    1 -> branch {
        same: -> identity
        same: -> identity
    } => result
}
