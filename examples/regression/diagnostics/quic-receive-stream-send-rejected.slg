import std.net.quic as quic

invalidSend owner: mut quic.ReceiveStream, connection: mut quic.Connection, endpoint: mut quic.Endpoint -> Unit uses Network, Clock {
    owner -> send(connection, endpoint, [1])
}

main {}
