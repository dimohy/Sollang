import sys.socket as socket

main {
    socket.ListenOptions {
        endpoint: socket.Endpoint { address: "127.0.0.1", port: UInt16(8080) }
        backlog: 8
        reuseAddress: true
    } -> socket.listen -> when {
        Ok(listener) {}
        Err(error) {}
    }
}
