import std.net.socket as socket
import std.net as net

main {
    socket.ListenOptions {
        endpoint: net.loopback(UInt16(8080)) -> unified
        backlog: 8
        reuseAddress: true
    } -> listen -> when {
        Ok(listener) {}
        Err(error) {}
    }
}
