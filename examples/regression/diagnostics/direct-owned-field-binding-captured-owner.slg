struct Owner { bytes: [UInt8; ~] }
struct Holder { owner: Owner }

inspect holder: Holder -> Int {
    run unit: Unit -> Int {
        holder.owner => moved
        moved.bytes -> len
    }
    Unit -> run
}

main {
    Holder { owner: Owner { bytes: [1; ~] } } -> inspect -> println
}
