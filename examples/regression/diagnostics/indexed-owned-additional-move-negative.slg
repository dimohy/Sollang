struct Entry { values: [Int; ~] }
consume index: Int, value: move Entry -> Int {
    value.values[index]
}
main {
    [Entry { values: [41, 42; ~] },; ~] => entries!
    0 -> consume(entries![0]) -> println
}
