# A local target cannot hide a non-sendable Arena capture inside a parallel branch worker.
run value: Int -> (left: Int, right: Int) {
    addUsage input: Int -> Int => input + Int(memory -> used)

    Arena(8) => memory
    value -> parallel branch {
        left: -> addUsage
        right: -> addUsage
    }
}

main {
    1 -> run => result
}
