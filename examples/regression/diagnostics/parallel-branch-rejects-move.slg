# A move arm cannot overlap another parallel arm.
struct Item {
    value: Int
}

consume item: move Item -> Int => item.value
inspect item: ref Item -> Int => item.value

main {
    Item { value: 4 } => item
    item -> parallel branch {
        consumed: move -> consume
        inspected: ref -> inspect
    } => results
}
