import std.net.quic as quic

invalidReceive owner: mut quic.SendStream, connection: mut quic.Connection, endpoint: mut quic.Endpoint -> Unit uses Network, Clock {
    owner -> receive(connection, endpoint, 1)
}

main {}
