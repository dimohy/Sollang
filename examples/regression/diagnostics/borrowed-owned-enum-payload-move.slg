struct Holder {
    value: Item
}

enum Item {
    Data([UInt8; ~])
    Empty
}

main {
    Holder { value: Item.Data([1, 2; ~]) } => holder
    holder.value -> when {
        Data(bytes) { Item.Data(bytes) => moved }
        Empty {}
    }
}
