# No later arm can start after the branch input has moved.
consume values: move [Int; ~] -> Int => values -> len
inspect values: ref [Int; ~] -> Int => values -> len

main {
    [1, 2; ~] => values!
    values! -> branch {
        consumed: move -> consume
        tooLate: ref -> inspect
    } => result
}
