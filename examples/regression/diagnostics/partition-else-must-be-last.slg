# A partition else route is unique and final.
import std.sequence

makeValues values: Range -> Stream<Int> => values -> defer

main {
    1..4 -> makeValues => values!
    values! -> partition value {
        other: else
        even: when value % 2 == 0
    } => routed!
}
