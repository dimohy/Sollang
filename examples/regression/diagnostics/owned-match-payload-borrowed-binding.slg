mutate result: Result<[Int; ~], Text> -> Int {
    result -> when {
        Ok(payload) {
            payload => owner!
            owner! -> push(2)
            owner! -> len
        }
        Err(_) { 0 }
    }
}

main {
    Result<[Int; ~], Text>.Ok([1; ~]) -> mutate -> println
}
