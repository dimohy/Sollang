struct Owner { bytes: [UInt8; ~] }

consume first: move Owner, second: move Owner -> Int => (first.bytes -> len) + (second.bytes -> len)

main {
    Owner { bytes: [1; ~] } => first
    Owner { bytes: [2; ~] } => second
    first -> consume(second) => count
    second.bytes -> len => reused
}
