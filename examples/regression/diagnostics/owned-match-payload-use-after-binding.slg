main {
    Result<[Int; ~], Text>.Ok([1; ~]) -> when {
        Ok(payload) {
            payload => owner!
            owner! -> push(2)
            payload -> len -> println
        }
        Err(error) { error -> println }
    }
}
