import std.text.glob as glob

main {
    glob.pattern("*", glob.Limits { maxPatternBytes: 1, maxInputBytes: 8, maxSteps: 32 }) -> when {
        Ok(matcher) { matcher.maxSteps -> println }
        Err(error) { error.patternOffset -> println }
    }
}
