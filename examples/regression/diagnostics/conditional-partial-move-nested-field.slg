struct Table {
    values: [Int; ~]
}

struct Packet {
    output: [Int; ~]
    table: Table
}

consume packet: move Packet, condition: Bool -> Unit {
    condition -> if {
        packet.table => moved
    }
}

main {
    Packet {
        output: [1]
        table: Table { values: [2] }
    } => packet
    consume(packet, true)
}
