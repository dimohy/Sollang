# Returning borrowed affine elements requires an explicit ownership transfer.
struct Item { values: [Int; ~] }
copy items: [Item; 1] -> [Item; 1] { items }
main {
    [Item { values: [13; ~] }] => items
    items -> copy => copied
    copied[0].values[0] -> println
}
