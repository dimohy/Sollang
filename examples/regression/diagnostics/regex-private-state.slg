import std.text.regex as regex

main {
    regex.pattern("a", regex.Limits { maxPatternBytes: 1, maxAtoms: 1, maxInputBytes: 1, maxSteps: 10 }) -> when {
        Ok(matcher) { matcher.maxSteps -> println }
        Err(error) { error.patternOffset -> println }
    }
}
