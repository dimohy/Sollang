# A partition consumer must name one of the declared routes.
import std.sequence

makeValues values: Range -> Stream<Int> => values -> defer

main {
    1..3 -> makeValues => values
    values -> partition value {
        even: when value % 2 == 0
        other: else
    } => routed
    routed.missing -> each value {
        value -> println
    }
}
