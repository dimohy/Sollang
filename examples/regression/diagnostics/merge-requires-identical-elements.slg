# Merge has one output edge and therefore requires one common element type.
import std.sequence

struct Token {
    value: Int
}

main {
    1..3 -> defer => numbers
    1..3 -> map value {
        Token { value: value }
    } => tokens
    (numbers, tokens) -> merge => invalid
}
