struct Carry {
    original: [UInt8; 32]
    derived: [UInt8; 16]
}

carry original: [UInt8; 32] -> Carry {
    Carry { original: original, derived: [7; 16] }
}

main {
    [3; 32] -> carry => value
    "fixed-carry=$(value.original[0]),$(value.original[31]),$(value.derived[15])" -> println
}
