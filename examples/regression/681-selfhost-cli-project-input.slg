import sollang.compiler.cli_project as cliProject

main {
    Arena(0) => manifestStorage!
    Arena(0) => rootStorage!
    "examples/regression/projects/272-project-manifest" -> cliProject.load("", manifestStorage!, rootStorage!) => input!
    "cli project = $(input!.status),$(input!.source)" -> println
}
