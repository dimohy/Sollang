# Entry interpolation preserves the UInt8-to-Int conversion before division.
struct Packet {
    bytes: [UInt8; 4]
}

main {
    Packet { bytes: [200, 200, 200, 200] } => packet
    "half=$(Int(packet.bytes[2]) / 2)" -> println
}
