# Verifies vertical flow continuation and predictable fold names.
square: Int -> Int => it * it

increment: Int -> Int => it + 1

main {
    3
        -> square
        -> increment
        => transformed

    1..4
        -> fold 0 {
            acc + it
        }
        => total

    "transformed=$transformed, total=$total" -> println
}
