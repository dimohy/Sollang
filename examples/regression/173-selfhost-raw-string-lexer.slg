import sollang.compiler.lexer as lexer

main {
    """"
    """
    alpha
    beta
    """
    """" -> lexer.lex => tokens!
    tokens! -> each token {
        "raw token = $(token.kind),$(token.span.start),$(token.span.length)" -> println
    }
}
