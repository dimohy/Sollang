struct Owner { values: [UInt8; 1] }
borrow owner: ref Owner -> ref Owner { owner }
read owner: ref Owner -> UInt8 { owner.values[0] }
main { Owner { values: [17] } => named
    named -> borrow -> read -> println
    named -> read -> println }
