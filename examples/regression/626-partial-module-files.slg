# Multiple files may contribute declarations to one logical namespace without
# wrappers, imports between fragments, or runtime indirection.
import partial.module as module

main {
    20 -> module.twicePlusTwo => result
    "partial module = $result" -> println
}
