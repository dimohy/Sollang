# A chained call result keeps its exact nominal type, so an unrelated imported
# inherent method with the same name cannot override this lexical function.
import std.net as net
import std.net.socket as socket

send endpoint: net.Endpoint -> Int {
    7
}

main {
    net.loopback(80) -> unified -> send -> println
}
