import sollang.compiler.llvm.text as llvm

# Integer literals are emitted directly in the declared return width.
main {
    [
        """
        wideZero: -> UInt64 => 0

        main {
            wideZero() -> println
        }
        """; ~
    ] => sources
    sources -> llvm.emit
}
