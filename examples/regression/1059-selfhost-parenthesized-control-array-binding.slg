# A parenthesized value-producing control expression must bind to the completed
# aggregate result, not to the condition hidden inside transparent wrappers.
main {
    ((true -> if {
        [40; ~]
    } else {
        [0; ~]
    })) => values
    values[0] + 2 -> println
}
