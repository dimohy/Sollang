# Repeated borrowing preserves the named owner until its scope ends.
struct Owner { values: [Int; ~] }
create: -> Owner { Owner { values: [13; ~] } }
impl Owner {
    first: self -> Int { self.values[0] }
}
main {
    create() => owner
    owner -> first -> println
    owner -> first -> println
}
