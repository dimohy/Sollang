struct Collector {
    value: Int
}

impl Collector {
    public collect: self -> Int => self.value
}

# An unrelated instance collect method must not select process capture runtime.
main {
    Collector { value: 42 } -> collect => value
    "$value" -> println
}
