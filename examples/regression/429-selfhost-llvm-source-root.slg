import sollang.compiler.llvm.source_root as llvmRoot
import sys.path as path

main {
    "examples/regression/fixtures/429-selfhost-root" -> path.fromText(path.nativeStyle()) => root!
    root! -> llvmRoot.emitRoot
}
