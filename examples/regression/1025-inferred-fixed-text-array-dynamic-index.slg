# An inferred immutable fixed-array binding keeps the borrowed pointer/length
# expression ABI when its element is selected through a runtime index.
main {
    ["port", "flow", "scope"] => names
    1 => index
    "$(names[index])" -> println
}
