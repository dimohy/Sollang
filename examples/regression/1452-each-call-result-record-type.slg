import probe.nodes as nodes

main {
    ["source"; ~] -> nodes.lower => ir
    0 => count!
    ir -> each node {
        node.kind == 6 and node.symbol >= 0 -> if {
            count! + 1 => count!
        }
    }
    count! -> println
}
