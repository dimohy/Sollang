# Explicit boxes terminate by-value layout dependencies.
enum Tree { Leaf, Child(box Tree) }
main {
    Tree.Child(box Tree.Leaf) => tree
    tree -> when {
        Leaf { "leaf" -> println }
        Child(child) { "child" -> println }
    }
}
