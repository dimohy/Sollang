struct Owner {
    values: [Int; ~]
}

consume values: move [Owner; 2] -> Int {
    values -> len
}

main {
    # Moving the fixed owner preserves each element's cleanup obligation.
    [Owner { values: [7,; ~] }, Owner { values: [8, 9; ~] }] -> consume -> println
}