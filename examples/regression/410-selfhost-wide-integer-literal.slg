import sollang.compiler.llvm.text as llvm

main {
    [
        """
        wide: -> UInt64 => UInt64(15_528_987_723_426_532_771)

        main {
            wide() => value
        }
        """; ~
    ] => sources
    sources -> llvm.emit
}
