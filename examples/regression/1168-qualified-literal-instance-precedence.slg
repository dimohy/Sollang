# A typed instance method wins over unrelated imported globals with the same name.
import examples.qualified_method_process as process
import examples.qualified_method_tool as tool

main {
    process.Command { value: 42 } => command
    command -> run => value
    "$value" -> println
}
