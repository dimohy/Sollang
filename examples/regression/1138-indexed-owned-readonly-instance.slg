struct Owner {
    values: [Int; ~]
}

impl Owner {
    public count: self -> Int => self.values -> len
}

main {
    [Owner { values: [7,; ~] }, Owner { values: [8, 9; ~] }] => owners
    owners[0] -> count => count
    "count=$count" -> println
}
