count values: [Text; ~] -> Int => values -> len

main {
    "payload" => source
    [source,; ~] => values
    [Text; ~] => empty

    values[0] -> println
    "lengths=$(values -> count)/$(empty -> len)" -> println
}
