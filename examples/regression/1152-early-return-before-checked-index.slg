preferredOrFirst preferred: Text, fallback: [Text; ~] -> Text {
    (preferred -> len) > 0 -> if { preferred -> return }
    fallback[0]
}

main {
    [Text; ~] => empty
    "ready" -> preferredOrFirst(empty) -> println
}
