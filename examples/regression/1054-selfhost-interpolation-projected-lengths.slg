struct Buffers {
    left: [UInt8; ~]
    right: [UInt8; ~]
}

main {
    Buffers { left: [1, 2], right: [3, 4, 5] } => buffers
    "sizes=$(buffers.left -> len),$(buffers.right -> len)" -> println
}
