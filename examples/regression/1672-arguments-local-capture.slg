import sys.process as process

inspect arguments: Arguments -> Unit uses Console {
    show: -> Unit uses Console {
        (arguments -> len) -> println
        arguments[1] -> println
        arguments[2] -> println
    }
    show()
}

main { process.arguments() -> inspect }