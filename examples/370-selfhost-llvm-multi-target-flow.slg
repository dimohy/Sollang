import sollang.compiler.llvm.text as llvm

main {
    [
        """
        digit value: Int -> Text => "X"

        main {
            21 -> digit -> println
        }
        """,
        ~
    ] => sources!
    sources! -> llvm.emit
}
