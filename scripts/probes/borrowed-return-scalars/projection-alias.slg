testOutputName explicitSources: [Text; ~] -> Text {
    explicitSources[0] => source
    source -> slice(UIntSize(5), UIntSize(4))
}

main {
    ["root/name.slg"; ~] => sources
    sources -> testOutputName => name
    name -> println
}
