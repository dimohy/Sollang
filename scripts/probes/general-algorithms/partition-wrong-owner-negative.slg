import std.algorithm as algorithm

struct OtherAlgorithms { }

impl OtherAlgorithms {
    partitionInPlace<T>: self, values: mut [T; ~] -> Int block item: ref T -> Bool {
        0
    }
}

main {
    algorithm.Algorithms { } => algorithms
    [1, 2, 3; ~] => values!
    algorithms -> OtherAlgorithms.partitionInPlace(values!) item { item > 0 } => boundary
    boundary -> println
}
