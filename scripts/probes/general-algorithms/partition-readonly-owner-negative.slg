import std.algorithm as algorithm

main {
    algorithm.Algorithms { } => algorithms
    [1, 2, 3; ~] => values
    algorithms -> partitionInPlace(values) item { item > 0 } => boundary
    boundary -> println
}
