# Valid intended storage surface; the current managed artifact reports unknown T.
struct Ordering { }
impl Ordering {
    count<T>: self, values: [T] -> Int => values -> len
}
main {
    [1, 2; ~] => values
    Ordering { } -> count(values) -> println
}
