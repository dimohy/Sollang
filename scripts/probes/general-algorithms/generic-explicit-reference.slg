# Valid intended borrowed comparator surface; current resolution loses T.
struct Ordering { }
impl Ordering {
    observe<T>: self, value: ref T -> Int => 1
}
main {
    Ordering { } -> observe(2) -> println
}
