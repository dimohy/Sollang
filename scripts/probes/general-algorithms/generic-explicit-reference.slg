# Generic explicit references borrow an addressable owner without losing T.
struct Ordering { }
impl Ordering {
    observe<T>: self, value: ref T -> Int => 1
}
main {
    2 => value
    Ordering { } -> observe(value) -> println
}
