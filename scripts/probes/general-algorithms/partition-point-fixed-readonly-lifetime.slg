import std.algorithm as algorithm

main {
    algorithm.Algorithms { } => algorithms
    [1, 2, 3, 4] => values
    algorithms -> partitionPoint(values) item { item < 10 } => boundary
    "boundary=$boundary,first=$(values[0])" -> println
}
