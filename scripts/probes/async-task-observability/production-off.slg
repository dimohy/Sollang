namespace probes.async_task_observability.production_off

public work value: Int -> async Int {
    yield
    value + 1
}

main {
    41 -> probes.async_task_observability.production_off.work -> await -> println
}
