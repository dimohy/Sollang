namespace probes.async_task_observability.scheduler_control_negative

import std.async.diagnostics as diagnostics

main {
    # Diagnostic identities and snapshots are observations, not capabilities.
    diagnostics.resumeTask(diagnostics.TaskKey { sessionId: 1, taskId: 1 })
}
