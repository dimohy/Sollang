namespace probes.async_task_observability.ambient_registry_negative

import std.async.diagnostics as diagnostics

main {
    # No process-global allTasks/currentTasks lookup exists.
    diagnostics.allTasks() -> println
}
