# A borrowed generic argument requires an addressable owner or existing reference.
struct Ordering { }
impl Ordering {
    observe<T>: self, value: ref T -> Int => 1
}
main {
    Ordering { } -> observe(2) -> println
}
