struct Holder {
    bytes: [UInt8; 16]
}

fixedResult: -> [UInt8; 16] {
    [9; 16]
}

namedFixedCopyIntoAggregate: -> Holder {
    fixedResult() => bytes
    Holder { bytes: bytes }
}

main {
    namedFixedCopyIntoAggregate() => holder
    holder.bytes[0] -> println
}
