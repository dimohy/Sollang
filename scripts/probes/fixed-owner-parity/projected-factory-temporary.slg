struct BlockValue {
    bytes: [UInt8; 16]
}

block: -> BlockValue {
    BlockValue { bytes: [7; 16] }
}

first values: [UInt8; 16] -> UInt8 {
    values[0]
}

main {
    block().bytes -> first -> println
}
