import std.net.socket as socket

main {
    socket.CompletionReactorOptions {
        registrations: 4
        pendingOperations: 3
        completionBatch: 2
    } -> socket.completionReactor -> when {
        Ok(reactor) { reactor -> close }
        Err(error) { "unexpected=$(error.code)" -> println }
    }
}
