import std.net.socket as socket

main {
    [3; ~] -> socket.operationSlot(5, socket.OperationDirection.Send) => slot
    slot -> intoBytes => first
    slot -> intoBytes => second
    "$(first -> len),$(second -> len)" -> println
}
