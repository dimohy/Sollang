import sys.path as path

main {
    path.fromText("browser-path", path.Style.Posix) -> query -> when {
        Ok(info) { "path-query=unexpected-ok" -> println }
        Err(error) { "path-query=err,$error" -> println }
    }
}
