struct Algorithms { }

impl Algorithms {
    invalid<T>: self, value: T -> Bool block item: ref T -> Bool {
        self -> yield
    }
}

main {
    Algorithms { } -> invalid(1) item { item > 0 } => result
    result -> println
}
