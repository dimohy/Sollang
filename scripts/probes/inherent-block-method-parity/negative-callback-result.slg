struct Algorithms { }

impl Algorithms {
    accepts<T>: self, value: T -> Bool block item: ref T -> Bool {
        value -> yield
    }
}

main {
    Algorithms { } -> accepts(1) item { item + 1 } => result
    result -> println
}
