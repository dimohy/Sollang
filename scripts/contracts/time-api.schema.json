{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "$schema",
    "version",
    "module",
    "runtimeModule",
    "officialReferences",
    "implementedSurfaces",
    "invariants",
    "orderedSlices",
    "targetPolicy"
  ],
  "properties": {
    "$schema": { "const": "time-api.schema.json" },
    "version": { "const": 1 },
    "module": { "const": "std.time" },
    "runtimeModule": { "const": "sys.runtime" },
    "officialReferences": {
      "type": "array",
      "minItems": 4,
      "uniqueItems": true,
      "items": { "type": "string", "format": "uri" }
    },
    "implementedSurfaces": {
      "type": "array",
      "minItems": 14,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 3 }
    },
    "invariants": {
      "type": "array",
      "minItems": 10,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 12 }
    },
    "orderedSlices": {
      "type": "array",
      "minItems": 6,
      "maxItems": 6,
      "items": { "$ref": "#/$defs/slice" }
    },
    "targetPolicy": {
      "type": "object",
      "additionalProperties": false,
      "required": ["windows-x64", "linux-x64", "wasm32-browser"],
      "properties": {
        "windows-x64": { "type": "string", "minLength": 12 },
        "linux-x64": { "type": "string", "minLength": 12 },
        "wasm32-browser": { "type": "string", "minLength": 12 }
      }
    }
  },
  "$defs": {
    "slice": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "status", "title"],
      "properties": {
        "id": { "type": "string", "pattern": "^T[1-6]$" },
        "status": { "enum": ["pending", "in-progress", "complete"] },
        "title": { "type": "string", "minLength": 5 },
        "requiredSurfaces": {
          "type": "array",
          "minItems": 2,
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 3 }
        },
        "requiredConsumers": {
          "type": "array",
          "minItems": 4,
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 3 }
        }
      },
      "oneOf": [
        { "required": ["requiredSurfaces"] },
        { "required": ["requiredConsumers"] }
      ]
    }
  }
}
