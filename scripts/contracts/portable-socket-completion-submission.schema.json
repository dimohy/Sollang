{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["$schema", "schemaVersion", "blockerId", "status", "target", "fixture", "expected", "pendingFixture", "pendingExpected", "required", "forbidden"],
  "properties": {
    "$schema": { "const": "portable-socket-completion-submission.schema.json" },
    "schemaVersion": { "const": 1 },
    "blockerId": { "const": "PORTABLE_SOCKET_COMPLETION_SUBMISSION" },
    "status": { "enum": ["focused-pending", "managed-windows-focused-complete"] },
    "target": { "const": "windows-x64" },
    "fixture": { "const": "scripts/probes/portable-memory-io/socket-completion-submission-windows.slg" },
    "expected": { "const": "scripts/probes/portable-memory-io/socket-completion-submission-windows.stdout.txt" },
    "pendingFixture": { "const": "scripts/probes/portable-memory-io/socket-completion-pending-wake-windows.slg" },
    "pendingExpected": { "const": "scripts/probes/portable-memory-io/socket-completion-pending-wake-windows.stdout.txt" },
    "required": { "type": "array", "minItems": 5, "uniqueItems": true, "items": { "type": "string", "minLength": 1 } },
    "forbidden": { "type": "array", "minItems": 5, "uniqueItems": true, "items": { "type": "string", "minLength": 1 } }
  }
}
