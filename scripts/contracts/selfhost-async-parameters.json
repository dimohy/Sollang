{
  "schemaVersion": 1,
  "scope": "selfhost affine Task scalar parameter transfer",
  "fixtures": [
    "examples/regression/225-structured-async.slg",
    "scripts/contracts/fixtures/c415-async-scalar-parameters.slg",
    "scripts/contracts/fixtures/c415-async-owned-parameter.slg"
  ],
  "supportSourceRoots": [
    "scripts/contracts/fixtures/c416-standalone-root"
  ],
  "invariants": [
    "the Task context keeps the result at offset zero and places every input in an aligned later slot",
    "the async wrapper stores each primary and additional input before publishing the Task",
    "the worker reloads each input and passes it to the exact async body parameter",
    "two tasks started with different arguments retain independent input values",
    "an affine owned input reaches the async body and completes without invalid cleanup on the normal path",
    "exact native output is required because LLVM assembly alone permits behaviorally wrong call shapes",
    "a standalone async program emits the complete Task scheduler without requiring the stdlib runtime module"
  ],
  "deferred": [
    "closure captures",
    "owned-input cancellation before first execution",
    "multiple suspension states and typed spill frames"
  ]
}
