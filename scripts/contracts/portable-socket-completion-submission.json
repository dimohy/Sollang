{
  "$schema": "portable-socket-completion-submission.schema.json",
  "schemaVersion": 1,
  "blockerId": "PORTABLE_SOCKET_COMPLETION_SUBMISSION",
  "status": "managed-windows-focused-complete",
  "target": "windows-x64",
  "fixture": "scripts/probes/portable-memory-io/socket-completion-submission-windows.slg",
  "expected": "scripts/probes/portable-memory-io/socket-completion-submission-windows.stdout.txt",
  "pendingFixture": "scripts/probes/portable-memory-io/socket-completion-pending-wake-windows.slg",
  "pendingExpected": "scripts/probes/portable-memory-io/socket-completion-pending-wake-windows.stdout.txt",
  "required": [
    "synchronous receive returns Completed without recording a pending operation",
    "pending receive transfers the exact caller buffer and wakes an awaited Task through dequeue",
    "cancel requests CancelIoEx but dequeue of the IOCP packet selects the sole terminal winner",
    "terminal slots recycle exactly once and retain the original allocation",
    "reactor close cancels and drains pending native operations before dropping each retained owner",
    "one bounded reactor-owned IOCP waiter publishes completions to the scheduler-owned common IO queue"
  ],
  "forbidden": [
    "per-operation blocking helper thread or scheduler Task worker blocking on GetQueuedCompletionStatusEx",
    "busy polling",
    "payload memcpy or memmove",
    "unbounded native tables",
    "success fallback after native failure"
  ]
}
