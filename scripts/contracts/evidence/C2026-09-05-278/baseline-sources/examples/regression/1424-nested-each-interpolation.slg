show values: ref [Int; ~], enabled: Bool -> Unit uses Console {
    enabled -> if {
        values -> each item { "item=$item" -> println }
    } else {
        "off" -> println
    }
}

main {
    [11, 22; ~] => values
    values -> show(true)
    values -> show(false)
}
