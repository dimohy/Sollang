{
  "threshold": "6/6; no fabricated output or historical trace overwrite",
  "frozenBeforeImplementation": true,
  "cases": [
    {
      "id": "actual-success",
      "expected": "actual executed output hashes, schema and evaluator accepted, focused scope, overall goal false"
    },
    {
      "id": "unexpected-exit",
      "expected": "reject without trace"
    },
    {
      "id": "input-drift-before",
      "expected": "reject before execution"
    },
    {
      "id": "input-drift-during",
      "expected": "reject without trace"
    },
    {
      "id": "forged-authority",
      "expected": "reject caller authority"
    },
    {
      "id": "baseline-wrapper-failure",
      "expected": "reject same exit1 with wrong stdout/stderr; never call wrapper error expected baseline"
    }
  ],
  "schemaVersion": 1
}
