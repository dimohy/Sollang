{
  "schemaVersion": 1,
  "status": "compiler-prerequisite",
  "scope": "generic-inherent-block-method-exact-receiver-and-declared-role-specialization",
  "authority": "D394 generic inherent methods retain trait, ownership, block-callback, specialization, and O0 ABI parity",
  "invariants": {
    "resolution": "unqualified block-method syntax resolves by the source value exact static receiver owner before same-name candidates",
    "callbackInput": "generic specialization uses the declared block role ref T, not self or the receiver type",
    "receiverSeparation": "self is the method receiver and never becomes the callback item solely because the declaration is inherent",
    "fallback": "no top-level global function or wrong-owner method fallback",
    "ownership": "readonly ref T callback accepts a noncopyable element without move, copy, retain, or owner loss"
  },
  "positive": {
    "id": "P1",
    "source": "scripts/probes/inherent-block-method-parity/positive-exact-owner-role.slg",
    "expected": "scripts/probes/inherent-block-method-parity/positive-exact-owner-role.stdout.txt"
  },
  "negative": [
    {
      "id": "N1",
      "source": "scripts/probes/inherent-block-method-parity/negative-wrong-owner.slg",
      "expected": "scripts/probes/inherent-block-method-parity/negative-wrong-owner.stderr.contains.txt"
    },
    {
      "id": "N2",
      "source": "scripts/probes/inherent-block-method-parity/negative-declared-role.slg",
      "expected": "scripts/probes/inherent-block-method-parity/negative-declared-role.stderr.contains.txt"
    },
    {
      "id": "N3",
      "source": "scripts/probes/inherent-block-method-parity/negative-callback-result.slg",
      "expected": "scripts/probes/inherent-block-method-parity/negative-callback-result.stderr.contains.txt"
    }
  ],
  "acceptance": "positive exact native output with zero diagnostics; every negative fails during managed semantics with one required diagnostic and no executable or LLVM artifact",
  "knownFailure": {
    "unqualified": "unknown block function 'partitionPoint'",
    "qualified": "yield expects ref Algorithms but received Algorithms"
  },
  "selfhostImplementation": {
    "resolutionSource": "selfhost/semantic/call_resolution_sources.slg",
    "specializationSource": "selfhost/semantic/expression_types.slg",
    "receiverContract": "kind-48 block calls enter exact nominal receiver lookup and select only a matching block method",
    "roleContract": "inherent block methods specialize the declared first explicit input against the first explicit call argument; self remains dispatch-only",
    "validation": "formatter and static contract only; selfhost execution remains pending serial integration"
  }
}
