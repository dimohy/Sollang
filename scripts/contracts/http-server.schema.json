{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["$schema", "version", "module", "sources", "fixture", "endpointFixture", "bodyFixture", "emptyBodyFixture", "persistentFixture", "officialReferences", "surfaces", "invariants", "unsupported"],
  "properties": {
    "$schema": { "const": "http-server.schema.json" },
    "version": { "const": 4 },
    "module": { "const": "std.net.http.server" },
    "sources": {
      "type": "array",
      "minItems": 5,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 }
    },
    "fixture": { "const": "1240-http-one-request-server" },
    "endpointFixture": { "const": "1335-http-server-local-endpoint" },
    "bodyFixture": { "const": "1338-http-request-body-cursor" },
    "emptyBodyFixture": { "const": "1339-http-empty-request-body" },
    "persistentFixture": { "const": "1341-http-persistent-pipeline" },
    "officialReferences": {
      "type": "array",
      "minItems": 6,
      "uniqueItems": true,
      "items": { "type": "string", "format": "uri" }
    },
    "surfaces": {
      "type": "array",
      "minItems": 18,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 }
    },
    "invariants": {
      "type": "array",
      "minItems": 29,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 }
    },
    "unsupported": {
      "type": "array",
      "minItems": 4,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 }
    }
  }
}
