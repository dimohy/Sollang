{
  "$schema": "typed-block-mutable-borrow.schema.json",
  "schemaVersion": 1,
  "status": "production-source-fixed-integrated-candidate-pending",
  "authority": "typed-block functions register primary and additional mutable-borrow inputs through one shared compiler path",
  "compilerSource": "src/Sollang.Compiler/Semantics/SemanticCompiler.cs",
  "helper": "MutableBorrowBindingNames",
  "staticResultSchema": "scripts/contracts/typed-block-mutable-borrow-static-result.schema.json",
  "managedResultSchema": "scripts/contracts/typed-block-mutable-borrow-managed-result.schema.json",
  "cases": [
    { "id": "P-PRIMARY-MUT", "kind": "compile-success", "source": "scripts/probes/typed-block-mutable-borrow/primary-mutable-receiver.slg" },
    { "id": "P-ADDITIONAL-MUT", "kind": "compile-success", "source": "scripts/probes/typed-block-mutable-borrow/additional-mutable-parameter.slg" },
    { "id": "N-ADDITIONAL-READONLY", "kind": "compile-failure", "source": "scripts/probes/typed-block-mutable-borrow/additional-readonly-negative.slg", "expected": "scripts/probes/typed-block-mutable-borrow/additional-readonly-negative.stderr.contains.txt" }
  ],
  "acceptance": "production source has one shared helper used by ordinary and typed-block validation; both mutable cases compile warning-note-free; readonly additional input fails with exactly one semantic diagnostic before LLVM"
}
