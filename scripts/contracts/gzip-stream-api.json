{
  "$schema": "gzip-stream-api.schema.json",
  "version": 3,
  "module": "std.compress.gzip",
  "source": "stdlib/std/compress/gzip.slg",
  "fixture": "1214-gzip-transactional-streaming-decoder",
  "boundaryFixture": "1215-gzip-incremental-byte-boundaries",
  "surfaces": [
    "DecoderLimits.maxInputBytes",
    "DecoderLimits.maxMembers",
    "Codec.decoder",
    "Codec.decompressMembers",
    "Decoder.write",
    "Decoder.finish"
  ],
  "invariants": [
    "caller output changes only after every accepted member validates",
    "input and member limits fail before caller-output mutation",
    "member boundaries come from the aligned final DEFLATE block cursor",
    "single-member decompress retains exact trailing-data rejection",
    "decoder state is affine and finish consumes the instance",
    "write carries bit Huffman history checksum and member state without retaining complete compressed input",
    "stored fixed dynamic and concatenated members pass one-byte write boundaries",
    "decompressMembers retains the measured bulk path while fixture 1214 enforces one-shot and streaming semantic parity"
  ]
}
