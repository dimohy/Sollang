struct Ordering { }
impl Ordering {
    copy<T>: self, value: ref T -> T => value
}

main {
    42 => number
    "text" => text
    Ordering { } -> copy(number) -> println
    Ordering { } -> copy(text) -> println
}
