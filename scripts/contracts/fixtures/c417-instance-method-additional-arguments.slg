struct Counter {
    value: Int
}

impl Counter {
    weighted: self, scale: Int, offset: Int -> Int {
        self.value * scale + offset
    }
}

main {
    Counter { value: 5 } => counter
    counter.weighted(6, 2) => result
    "method=$(result)" -> println
}
