consumeOwned marker: Int, scores: move {Int: Int} -> async Int {
    marker + (scores -> len)
}

main {
    {Int: Int} => scores!
    scores! -> put(7, 70)
    30 -> consumeOwned(scores!) => task
    task -> await => result
    "owned=$(result)" -> println
}
