struct Ordering { }
impl Ordering {
    count<T>: self, values: [T] -> Int => values -> len
}

main {
    [1, 2; ~] => numbers
    ["one", "two"] => words
    Ordering { } -> count(numbers) -> println
    Ordering { } -> count(words) -> println
}
