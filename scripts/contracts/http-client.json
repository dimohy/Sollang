{
  "$schema": "http-client.schema.json",
  "version": 1,
  "module": "std.net.http.client",
  "sources": [
    "stdlib/std/net/http/client.slg",
    "stdlib/std/net/http/request.slg",
    "stdlib/std/net/http/body.slg",
    "stdlib/std/net/http.slg",
    "stdlib/std/net/socket.slg"
  ],
  "fixture": "1386-http-client-connection-reuse",
  "officialReferences": [
    "https://www.rfc-editor.org/rfc/rfc9110.html",
    "https://www.rfc-editor.org/rfc/rfc9112.html",
    "https://learn.microsoft.com/en-us/dotnet/fundamentals/networking/http/httpclient-guidelines",
    "https://learn.microsoft.com/en-us/dotnet/api/system.net.http.socketshttphandler",
    "https://pkg.go.dev/net/http"
  ],
  "surfaces": [
    "ClientOptions.connect",
    "Client.localEndpoint",
    "Client.remoteEndpoint",
    "Client.send",
    "Client.close",
    "Exchange.receive",
    "Response.status",
    "Response.reasonText",
    "Response.fieldCount",
    "Response.fieldName",
    "Response.fieldValue",
    "Response.openBody",
    "ResponseBody.readInto",
    "ResponseBody.finish",
    "ResponseOutcome.Reusable"
  ],
  "invariants": [
    "all public stateful operations are inherent instance methods",
    "one affine Client owns exactly one TCP transport",
    "send consumes Client and returns one outstanding Exchange",
    "request head body length must equal caller-owned body storage length",
    "response semantics are retained from the validated outgoing request",
    "response head and response body use separate owned buffers",
    "response head storage remains stable while the body buffer is refilled",
    "response framing is selected by the shared body policy",
    "connection tokens are parsed case-insensitively as comma-separated field values",
    "HTTP 1.1 is persistent unless either peer requests close",
    "HTTP 1.0 is reusable only after explicit keep-alive",
    "until-close framing requires socket EOF and is never reusable",
    "finish rejects incomplete bodies",
    "finish rejects unread bytes before returning a reusable Client",
    "a reusable Client is returned only after the response body is complete",
    "closed responses close their transport before reporting Closed",
    "DNS TLS redirects decompression and protocol fallback are not hidden",
    "the fixture proves two sequential exchanges use one accepted TCP connection"
  ],
  "unsupported": [
    "DNS name resolution",
    "TLS negotiation and certificate policy",
    "redirect policy",
    "automatic decompression",
    "timeouts and cancellation",
    "informational response chaining",
    "HTTP/2 and HTTP/3 framing",
    "connection pooling across endpoints"
  ]
}
