library fixture from "../../artifacts/native-interop/sollang_fixture"

main {
    fixture.add(20, 22) => answer
    "$answer" -> println

    fixture.multiply(6, 7) => product
    "$product" -> println

    fixture.hypotSquared(3, 4) => squared
    Int64(squared) => squaredInt
    "$squaredInt" -> println
}
