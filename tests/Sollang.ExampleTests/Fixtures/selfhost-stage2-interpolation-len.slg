# Parenthesized interpolation evaluates the complete flow call and prints the
# current mutable-array length, not the array storage value.
main {
    [1, 2, 3; ~] => values!
    values! -> push(4)
    "count=$(values! -> len)" -> println
}
