namespace regression.unrelated_intrinsic_methods

struct Counter { value: Int }

impl Counter {
    count: self -> Int { self.value }
    len: self -> Int { self.value }
}

# A private root function must not shadow an intrinsic in this module.
public builtinLength: -> Int { [3, 5] -> len }
