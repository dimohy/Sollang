# An if-expression branch ending in a flow intrinsic yields that intrinsic's
# value, not the flow source that precedes it.
selectLength left: Text, right: Text -> UIntSize {
    (left -> len) < (right -> len) -> if { left -> len } else { right -> len }
}

main {
    "abcd" -> selectLength("xy") -> println
}
