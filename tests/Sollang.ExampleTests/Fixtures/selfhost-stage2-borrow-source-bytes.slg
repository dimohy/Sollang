import sys.file as file

main {
    [UInt8(65), UInt8(10); ~] => bytes!
    bytes! -> file.borrowBytes => source!
    source! -> len => length
    source! -> slice(UIntSize(0), length) => text
    text -> print
}
