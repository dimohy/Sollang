enum Style {
    Posix
    Windows
}

select style: Style -> Bool {
    style -> when {
        Posix { true }
        Windows {
            false => selected!
            selected!
        }
    } => result
    result -> if {
        true -> return
    }
    false
}

main {
    Style.Posix -> select -> println
}
