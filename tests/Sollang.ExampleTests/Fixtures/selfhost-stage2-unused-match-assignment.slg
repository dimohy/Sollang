# A statement-position match may have assignment expressions in its arms.
# It must execute the arms without allocating or storing an unused match result.
main {
    false => seen!
    Result<Int, Text>.Ok(1) -> when {
        Ok(_) { true => seen! }
        Err(_) {}
    }
    seen! -> if { "ok" -> println }
}
