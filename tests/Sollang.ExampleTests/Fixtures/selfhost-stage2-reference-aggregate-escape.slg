# A returned struct cannot hide a readonly reference to callee-owned storage.
struct PairView {
    value: ref Int
}

same value: ref Int -> ref Int {
    value
}

escape: -> PairView {
    10 => local
    PairView { value: local -> same }
}

main {
    escape => escaped
}
