import sys.input.mouse

# Compile-only source: the stream is never pulled, so the gate proves the
# complete Source -> EventStream ABI without waiting for user input.
main {
    mouse.Source {
        capacity: 32
        overflow: mouse.OverflowPolicy.CoalesceMotion
    } -> events => mouseEvents
}
