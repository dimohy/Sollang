import std.alpha as alpha
import std.beta as beta

main {
    UInt64(10) -> alpha.encode => alphaValue
    UInt64(10) -> beta.encode => betaValue
    UInt64(10) -> alpha.localEncode => localValue
    alphaValue == 11 and betaValue == 12 and localValue == 11
        -> unless { 1 / 0 => failure }
}
