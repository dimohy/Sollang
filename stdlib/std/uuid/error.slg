namespace std.uuid.error

public enum Kind {
    InvalidLength
    InvalidPrefix
    InvalidSeparator
    InvalidHexDigit
    RandomUnavailable
    ClockBeforeUnixEpoch
    TimestampOverflow
    InvalidEntropyLength
}
