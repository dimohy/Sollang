namespace std.uri.error

public enum Kind {
    InvalidLimit
    InvalidCharacter
    InvalidScheme
    InvalidPercentEncoding
    InvalidAuthority
    InvalidHost
    InvalidPort
    RelativeFirstSegmentColon
}
