namespace std.compress.zstd.decoder_phase

public enum Phase {
    Magic
    Descriptor
    Window
    Dictionary
    ContentSize
    BlockHeader
    BlockRaw
    BlockRle
    BlockCompressed
    Checksum
    SkippableSize
    SkippableData
}
