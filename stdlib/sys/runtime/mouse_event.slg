namespace sys.input.mouse

# Runtime ABI for a validated Source. The implementation consumes the source
# plan and returns the affine stream that owns subscription cancellation.
impl Source {
    public events: move self -> EventStream<Event> uses Console = intrinsic
}
