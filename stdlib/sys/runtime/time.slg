namespace std.time

# Runtime ABI fragment for std.time. Duration remains the public value type in
# std/time.slg; this method is its direct intrinsic implementation.
impl Duration {
    public sleep: move self -> async Unit uses Clock = intrinsic
}
