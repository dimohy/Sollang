namespace std.net.dns

import std.net as net

# Runtime ABI fragment for std.net.dns. The receiver and result types remain
# public std values while this declaration binds the platform resolver directly.
impl Resolver {
    public lookup: self, host: Text, port: UInt16 -> Result<[net.Endpoint; ~], Error> uses Network = intrinsic
}
