namespace std.sequence

# Runtime ABI fragment for std.sequence. This remains the same logical
# namespace, so defer is lowered directly and introduces no wrapper frame.
public defer values: Range -> Stream<Int> = intrinsic
