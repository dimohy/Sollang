namespace sys.quic.tls_server_phase

public enum Phase {
    SendServerHello
    SendEncryptedExtensions
    SendCertificate
    SendCertificateVerify
    SendServerFinished
    AwaitClientFinished
    Connected
    Failed
}
