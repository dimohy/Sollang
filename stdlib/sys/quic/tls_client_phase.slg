namespace sys.quic.tls_client_phase

public enum Phase {
    AwaitServerHello
    AwaitEncryptedExtensions
    AwaitCertificate
    AwaitCertificateVerify
    AwaitServerFinished
    SendClientFinished
    Connected
    Failed
}
