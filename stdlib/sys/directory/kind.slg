namespace sys.directory.kind

public enum Kind {
    File
    Directory
    Symlink
    Other
}

public code kind: Kind -> Int => kind -> when {
    File => 0
    Directory => 1
    Symlink => 2
    Other => 3
}
